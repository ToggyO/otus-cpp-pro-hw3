#include <gtest/gtest.h>

#include "allocators/free_list_allocator.h"

// TODO: тесты констуркторов, тесты операторов сравнения
TEST(FreeListAllocatorTest, AllocationAndProperAlignment) {
//    int alignment = 8;
//    const std::size_t mem_size_bytes = 1000;
//
//    FreeListAllocator<mem_size_bytes> allocator;
//
//    allocator.allocate(10, alignment);
//
//    allocator.allocate(17, alignment);
}